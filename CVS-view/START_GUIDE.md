# 🎬 演唱会视频分享平台 - 启动指南

## 📋 前置要求

- **Node.js 16+** - [下载地址](https://nodejs.org/)
- **Java 17+** - 用于后端服务
- **MySQL 8.0+** - 数据库

## 🚀 快速启动

### 1. 启动后端服务

```bash
# 进入后端目录
cd CVS-port

# 运行后端服务
mvn spring-boot:run
```

后端将在 `http://localhost:8080` 启动

### 2. 安装前端依赖

**推荐方式：使用安装脚本（Windows）**
```bash
# 双击运行 CVS-view/install-dependencies.bat
# 或在命令行中运行：
cd CVS-view
.\install-dependencies.bat
```

**手动安装：**
```bash
cd CVS-view

# 清理旧依赖（如果有）
rm -rf node_modules package-lock.json

# 逐步安装依赖
npm install
npm install vue-router@^4.2.5
npm install pinia@^2.1.7
npm install axios@^1.6.0
npm install element-plus@^2.4.0
npm install @element-plus/icons-vue@^2.1.0
```

**一键安装所有依赖：**
```bash
cd CVS-view
npm run install:all
```

### 3. 启动前端服务

```bash
cd CVS-view
npm run dev
```

前端将在 `http://localhost:5173` 启动

## 🔧 数据库配置

1. **创建数据库**
```sql
-- 登录 MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE cvs_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 导入数据
source CVS-port/init.sql;
```

2. **修改配置**
编辑 `CVS-port/src/main/resources/application.properties`：
```properties
spring.datasource.username=你的用户名
spring.datasource.password=你的密码
```

## 📱 功能测试

### 测试账号

**管理员账号：**
- 用户名：`admin`
- 密码：`123456`

**普通用户账号：**
- 用户名：`user1` / `user2`
- 密码：`123456`

### 主要功能

1. **用户系统**
   - ✅ 注册新账号
   - ✅ 用户登录
   - ✅ 修改个人资料

2. **视频功能**
   - ✅ 浏览视频列表
   - ✅ 搜索和筛选
   - ✅ 观看视频详情
   - ✅ 上传视频（需审核）

3. **社交功能**
   - ✅ 发表评论
   - ✅ 点赞视频
   - ✅ 收藏视频

4. **管理员功能**
   - ✅ 审核视频
   - ✅ 管理用户
   - ✅ 查看统计数据

## 🐛 常见问题

### 依赖安装失败
```bash
# 清理缓存
npm cache clean --force

# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### 端口冲突
- 后端默认端口：8080
- 前端默认端口：5173

如有冲突，请修改 `vite.config.js` 中的端口配置。

### 数据库连接失败
1. 检查 MySQL 服务是否启动
2. 确认用户名和密码正确
3. 检查数据库是否存在

## 📂 项目结构

```
CVS-system/
├── CVS-port/                 # 后端项目
│   ├── init.sql             # 数据库初始化
│   └── src/main/java/cn/edu/seig/fnds/
│       ├── controller/      # REST控制器
│       ├── service/         # 业务逻辑
│       ├── mapper/          # 数据访问
│       ├── entity/          # 实体类
│       └── config/          # 配置类
└── CVS-view/                # 前端项目
    ├── install-dependencies.bat  # 依赖安装脚本
    ├── src/
    │   ├── views/           # 页面组件
    │   ├── stores/          # 状态管理
    │   ├── api/             # API接口
    │   └── main.js          # 应用入口
    └── package.json         # 项目配置
```

## 🎯 下一步

1. **开发环境**：项目已可以正常运行
2. **生产部署**：
   - 后端：`mvn clean package` 生成 JAR 包
   - 前端：`npm run build` 生成静态文件
   - 配置 Nginx 反向代理

3. **功能扩展**：
   - 视频播放器优化
   - 文件上传到云存储
   - 消息通知系统
   - 数据统计图表

## 📞 技术支持

如果遇到问题，请检查：
1. Node.js 和 Java 版本
2. 数据库连接配置
3. 依赖安装是否完整
4. 端口是否被占用

项目完全基于 Spring Boot + Vue3 技术栈，代码结构清晰，易于维护和扩展。