# 快速问题解决指南

## 🚨 后端启动问题

### ❌ 问题1: "Unable to find a single main class"
**错误信息**：
```
Unable to find a single main class from the following candidates 
[cn.edu.seig.fnds.CvsPortApplication, cn.edu.seig.fnds.CVSApplication]
```

**原因**：项目中有多个主启动类

**✅ 解决方案**：
已修复！多余的启动类已删除。现在可以正常启动。

重新运行：
```bash
cd CVS-port
mvn spring-boot:run
```

---

### ❌ 问题2: 数据库连接失败
**错误信息**：
```
Communications link failure
```

**✅ 解决方案**：
1. 检查MySQL服务是否启动
```bash
# Windows
sc query MySQL80
```

2. 检查数据库配置（application.properties）
```properties
spring.datasource.username=你的用户名
spring.datasource.password=你的密码
```

3. 确认数据库已创建
```sql
CREATE DATABASE cvs_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

### ❌ 问题3: 端口8080被占用
**错误信息**：
```
Port 8080 was already in use
```

**✅ 解决方案**：

**方法1**：修改端口
在 `application.properties` 添加：
```properties
server.port=8081
```

**方法2**：关闭占用端口的进程
```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F
```

---

### ❌ 问题4: "Invalid value type for attribute 'factoryBeanObjectType'"
**错误信息**：
```
java.lang.IllegalArgumentException: Invalid value type for attribute 'factoryBeanObjectType': java.lang.String
```

**原因**：MyBatis Plus 3.5.5与Spring Boot 3.2.1不兼容

**✅ 解决方案**：
已修复！MyBatis Plus版本已升级到3.5.7。

重新编译运行：
```bash
cd CVS-port
mvn clean install
mvn spring-boot:run
```

---

## 🎨 前端启动问题

### ❌ 问题4: npm install 失败
**错误信息**：
```
npm ERR! network timeout
```

**✅ 解决方案**：

**方法1**：使用安装脚本（推荐）
```bash
cd CVS-view
.\install-dependencies.bat
```

**方法2**：配置国内镜像
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

**方法3**：清理缓存重试
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

---

### ❌ 问题5: 前端页面空白
**可能原因**：后端未启动或API请求失败

**✅ 解决方案**：
1. 确保后端已启动（http://localhost:8080）
2. 打开浏览器开发者工具（F12）查看Console和Network
3. 检查是否有错误信息
4. 确认API请求地址正确

---

### ❌ 问题6: Element Plus组件不显示
**✅ 解决方案**：
```bash
npm install element-plus@^2.4.0
npm install @element-plus/icons-vue@^2.1.0
```

确保 `main.js` 中已引入：
```javascript
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
app.use(ElementPlus)
```

---

### ❌ 问题5: "Unknown column 'email' in 'field list'"
**错误信息**：
```
java.sql.SQLSyntaxErrorException: Unknown column 'email' in 'field list'
```

**原因**：数据库表结构与代码不一致，可能使用了旧版本的SQL脚本

**✅ 解决方案**：

**方法1**：重新导入数据库（推荐，会清空数据）
```bash
mysql -u root -p
DROP DATABASE IF EXISTS cvs_db;
source D:/BaiduSyncdisk/university/毕业/毕业设计/CVS-system/CVS-port/init.sql;
```

**方法2**：使用修复脚本（保留现有数据）
```bash
mysql -u root -p cvs_db < D:/BaiduSyncdisk/university/毕业/毕业设计/CVS-system/CVS-port/fix-database.sql
```

**方法3**：手动添加缺失列
```sql
USE cvs_db;
ALTER TABLE `user` ADD COLUMN `email` VARCHAR(100) COMMENT '邮箱' AFTER `nickname`;
ALTER TABLE `user` ADD UNIQUE KEY `uk_email` (`email`);
```

---

## 💾 数据库问题

### ❌ 问题7: 表不存在
**错误信息**：
```
Table 'cvs_db.user' doesn't exist
```

**✅ 解决方案**：
导入数据库脚本
```bash
mysql -u root -p
CREATE DATABASE cvs_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE cvs_db;
source D:/BaiduSyncdisk/university/毕业/毕业设计/CVS-system/CVS-port/init.sql;
```

---

## 🔐 功能问题

### ❌ 问题8: 无法登录
**✅ 解决方案**：
使用测试账号：
- 管理员：admin / 123456
- 普通用户：user1 / 123456

如果仍无法登录：
1. 检查数据库中user表是否有数据
2. 检查后端日志
3. 检查浏览器Console是否有错误

---

### ❌ 问题9: 上传视频后看不到
**原因**：视频需要管理员审核

**✅ 解决方案**：
1. 使用管理员账号登录（admin/123456）
2. 访问管理后台
3. 审核通过该视频

---

### ❌ 问题10: 点赞/收藏不生效
**✅ 解决方案**：
1. 确保已登录
2. 检查浏览器Console是否有错误
3. 检查后端日志
4. 尝试重新登录

---

## 🛠️ 开发问题

### ❌ 问题11: Maven编译失败
**✅ 解决方案**：
```bash
# 清理并重新编译
mvn clean install

# 跳过测试
mvn clean install -DskipTests
```

---

### ❌ 问题12: 热重载不生效
**后端**：
确保pom.xml中有devtools依赖

**前端**：
```bash
# 重启开发服务器
npm run dev
```

---

## 📞 仍然无法解决？

### 1. 查看详细文档
- [常见问题FAQ.md](./常见问题FAQ.md) - 更详细的问题解答
- [项目启动指南.md](./项目启动指南.md) - 完整启动步骤

### 2. 检查日志
- **后端日志**：控制台输出
- **前端日志**：浏览器Console（F12）
- **数据库日志**：MySQL错误日志

### 3. 验证环境
```bash
# 检查Java版本
java -version  # 应该是17+

# 检查Maven版本
mvn -version   # 应该是3.6+

# 检查Node版本
node -v        # 应该是16+

# 检查MySQL版本
mysql --version # 应该是8.0+
```

### 4. 完全重启
```bash
# 停止所有服务
# 关闭后端和前端的命令行窗口

# 重新启动
# 1. 启动MySQL
# 2. 启动后端
cd CVS-port
mvn spring-boot:run

# 3. 启动前端
cd CVS-view
npm run dev
```

---

## ✅ 快速检查清单

启动前检查：
- [ ] JDK 17+ 已安装
- [ ] Maven 3.6+ 已安装
- [ ] Node.js 16+ 已安装
- [ ] MySQL 8.0+ 已安装并运行
- [ ] 数据库cvs_db已创建
- [ ] init.sql已导入
- [ ] application.properties配置正确
- [ ] 端口8080和5173未被占用

---

**提示**：90%的问题都可以通过查看错误日志和本文档解决！

